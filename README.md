# Login-Registration-System

This is a basic login and registration system.
User can change password and delete account if wanted.
Uses SQLite to form a database and SHA-256 to perform password hashing.
